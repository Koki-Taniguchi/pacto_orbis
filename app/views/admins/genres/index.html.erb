<h1>ジャンル一覧</h1>

<div class="container">
  <div class="row">
    <div class="col-lg-3">
      <%= form_with(model: [:admins, @genre], local: true) do |f| %>
        <div>
          <%= f.label :name %><br>
          <%= f.text_field :name %>
        </div>
        <%= f.submit "登録", class: "btn btn-primary" %>
      <% end %>
    </div>
    <div class="col-lg-9">
      <table>
        <% @genres.each do |genre| %>
          <tr>
            <td><%= genre.name %></td>
            <td><%= link_to "編集", edit_admins_genre_path(genre) %></td>
            <td><%= link_to "削除", admins_genre_path(genre), method: :delete %></td>
          </tr>
        <% end %>
      </table>
    </div>
  </div>
</div>